{
  "name": "sitecombustao",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "http-server . -p 8080 -a 0.0.0.0",
    "test": "echo \"No tests configured\" && exit 0"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
